import { ErrorMessageContainer } from "./errorMessage.styles";

const ErrorMessage: React.FC = () => {
  return (
    <ErrorMessageContainer>Oops! Couldn't fetch data.</ErrorMessageContainer>
  );
};

export default ErrorMessage;
